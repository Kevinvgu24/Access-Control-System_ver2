version: '3.8'

services:
  smart-door-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: smart-door-server
    restart: always
    ports:
      - "5000:5000"
    volumes:
      - ./database:/app/database
