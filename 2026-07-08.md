# Nhật ký phiên làm việc ngày 08/07/2026

## 1. Đổi tên vai trò "Users" thành "Administrator" & Dọn dẹp giao diện
*   **Mục tiêu**: Đổi tên toàn bộ giao diện quản lý người dùng cũ thành "Administrator" (Người quản trị cấp phòng lab) và dọn dẹp các tính năng thừa.
*   **Các tệp đã chỉnh sửa**:
    *   `web_app/src/components/shell/Sidebar.tsx`: Đổi nhãn điều hướng của đường dẫn `/users` từ `Users` thành `Administrator`.
    *   `web_app/src/pages/UsersPage.tsx`: Đổi tên trang từ `User Directory` thành `Administrators`, thay đổi tiêu đề nhóm từ `The Roster` thành `Management`. Gỡ bỏ hoàn toàn nút bấm **Import Excel**, thẻ `input` file và toàn bộ logic import excel người dùng ở trang này.
    *   `web_app/src/pages/EnrollmentPage.tsx`: Cập nhật nút chuyển hướng hoàn tất thành `View Administrators`, đổi tiêu đề thành `Add New Administrator`, nút xác nhận thành `Confirm & Enroll Administrator`.

## 2. Phân chia lại Quyền hạn (Lecturer & Teacher Assistant)
*   **Mục tiêu**: Thay thế các vai trò cũ (`faculty`, `lab_assistant`) bằng bộ quyền mới phù hợp hơn cho quản lý phòng thực hành: **Lecturer** (Giảng viên) và **Teacher Assistant** (Trợ giảng).
*   **Các tệp đã chỉnh sửa**:
    *   `web_app/src/types/admin.ts`: Thay thế vai trò cũ trong kiểu dữ liệu `UserRole` thành:
        ```typescript
        export type UserRole = 'student' | 'lecturer' | 'teacher_assistant' | 'guest' | 'maintenance'
        ```
    *   `web_app/src/pages/UsersPage.tsx`: Cập nhật `ROLE_OPTS` và `ROLE_LABEL` để hiển thị nhãn bộ lọc và form chỉnh sửa thông tin ứng với `Lecturer` và `Teacher Assistant`.
    *   `web_app/src/pages/EnrollmentPage.tsx`: Cập nhật dropdown chọn vai trò khi đăng ký thành viên mới.

## 3. Chính sách truy cập cửa 24/7 (Chỉ cho phép Lecturer)
*   **Mục tiêu**: Thiết lập chính sách chỉ có vai trò **Lecturer** được phép vào phòng tự do 24/7 (bypass kiểm tra lịch học), các vai trò khác (học sinh, trợ giảng TA, kỹ thuật viên...) đều phải có lịch học mới được mở cửa.
*   **Các tệp đã chỉnh sửa**:
    *   `src/monitor_display/interface_monitor.py`: Cập nhật hàm `check_user_schedule_today()` trên thiết bị cửa:
        *   *Trước đây*: Miễn kiểm tra lịch cho bất kỳ ai không phải học sinh (`role != 'student'`).
        *   *Hiện tại*: Chỉ miễn kiểm tra lịch cho đúng vai trò `lecturer` (`if role == 'lecturer'`).

## 4. Quản lý phòng Lab (Switch Lab - Sửa trực tiếp & Xóa vĩnh viễn)
*   **Mục tiêu**: Bổ sung tính năng chỉnh sửa thông tin phòng lab ngay trên giao diện danh sách chọn phòng, đồng thời chuyển đổi cơ chế xóa phòng lab sang xóa vĩnh viễn dữ liệu (không giữ lại trạng thái ẩn/inactive).
*   **Các tệp đã chỉnh sửa**:
    *   `web_app/src/pages/LabSelectorPage.tsx`:
        *   Bổ sung nút bấm **✏️ Edit** bên cạnh nút Delete trên thẻ thông tin của từng phòng lab.
        *   Tích hợp form Modal **Edit Lab Room Details** cho phép cập nhật tên phòng, mã phòng, vị trí và người quản lý.
    *   `src/Newest_Version/api_server.py`:
        *   Cập nhật endpoint `/api/labs/<lab_id>/archive` để thực hiện xóa vĩnh viễn (Hard Delete) thay vì cập nhật trạng thái `status = 'inactive'`.
        *   Tự động xóa lan tỏa (Cascading Delete) toàn bộ các bản ghi liên quan đến phòng lab đó trong các bảng: `labs`, `clusters`, `nodes`, `lab_schedules`, `access_events`, và `incidents`.

## 5. Chọn lịch học theo File Excel tải lên (Schedules Page)
*   **Mục tiêu**: Thay thế việc chọn phòng lab bằng chọn danh sách file Excel đã được tải lên, hiển thị tên file Excel to và rõ ràng, hỗ trợ xóa/cập nhật từng danh sách riêng biệt.
*   **Các tệp đã chỉnh sửa**:
    *   `src/Newest_Version/database.py`: Thêm cột `filename TEXT` vào bảng `lab_schedules` và thiết lập câu lệnh di trú (`ALTER TABLE`) tự động cập nhật cơ sở dữ liệu trên startup.
    *   `src/Newest_Version/api_server.py`:
        *   Cập nhật route import `/api/labs/<lab_id>/schedules/import` để lưu lại tên file Excel gốc và chỉ xóa các bản ghi cũ của cùng file đó (cho phép nhiều file tồn tại song song).
        *   Thêm các API endpoints mới:
            *   `GET /api/schedules/files`: Lấy danh sách tất cả các file đã tải lên (gồm cả các danh sách cũ không có tên).
            *   `GET /api/schedules/by-file`: Lấy chi tiết lịch học theo tên file và ID phòng lab.
            *   `DELETE /api/schedules/by-file`: Xóa một danh sách file cụ thể.
    *   `web_app/src/pages/SchedulesPage.tsx`: Viết lại toàn bộ giao diện:
        *   Đổi dropdown "Select Lab" thành "Select Schedule List" hiển thị tên các file Excel đã tải lên kèm tên phòng lab tương ứng.
        *   Hiển thị tên danh sách lịch học kèm biểu tượng trang tài liệu `📄` nổi bật trên màn hình chính khi chọn.
        *   Cung cấp nút bấm **🗑️ Delete List** để xóa trực tiếp danh sách file đang chọn.
        *   Nút upload sẽ gửi file trực tiếp lên phòng lab hiện tại đang được chọn (Active Lab).

## 6. Gỡ bỏ mục Control Panel và Cảnh báo Docker
*   **Mục tiêu**: Loại bỏ hoàn toàn chức năng Control Panel khỏi giao diện người dùng và sửa cảnh báo docker-compose.
*   **Các tệp đã chỉnh sửa**:
    *   `web_app/src/components/shell/Sidebar.tsx`: Xóa bỏ nút liên kết điều hướng đến **Control Panel** khỏi menu Sidebar.
    *   `web_app/src/App.tsx`: Xóa bỏ import của `ControlPage` và định nghĩa Route `/control` trong danh sách các đường dẫn của ứng dụng.
    *   `docker-compose.yml`: Loại bỏ dòng `version: '3.8'` lỗi thời để tắt cảnh báo `the attribute version is obsolete` khi khởi động container trên LXC Docker.

## 7. Ràng buộc chọn phòng Lab trước khi truy cập trang khác
*   **Mục tiêu**: Ngăn người dùng truy cập các trang Dashboard, Administrator, Enrollment, Schedules, Logs, System nếu chưa chọn một phòng lab làm việc cụ thể.
*   **Các tệp đã chỉnh sửa**:
    *   `web_app/src/components/shell/Sidebar.tsx`:
        *   Tích hợp hàm kiểm tra giá trị `selectedLabId` trong sự kiện click của các `NavLink` trên Sidebar.
        *   Nếu chưa có phòng lab nào được chọn, ứng dụng sẽ chặn hành động chuyển trang (`e.preventDefault()`) và ghi nhận thông báo cảnh báo vào store dùng chung.
    *   `web_app/src/store/labStore.ts`: Bổ sung trạng thái `warning` tạm thời và hàm `setWarning()` để chia sẻ thông báo giữa các component mà không bị lưu vĩnh viễn (persistence exclusion).
    *   `web_app/src/pages/LabSelectorPage.tsx`:
        *   Lắng nghe trạng thái `warning` từ store.
        *   Hiển thị một banner cảnh báo màu vàng (amber) cực kỳ đẹp mắt ở phía trên cùng của trang Chọn phòng Lab kèm nút đóng (✕) tiện lợi thay vì dùng hộp thoại `alert` của trình duyệt.

## 8. Gỡ bỏ bảng Incidents khỏi Dashboard
*   **Mục tiêu**: Tối giản giao diện Dashboard bằng việc loại bỏ bảng theo dõi sự cố (Incidents).
*   **Các tệp đã chỉnh sửa**:
    *   `web_app/src/pages/OverviewPage.tsx`: Loại bỏ phần hiển thị panel **Incidents** khỏi cột bên phải của trang Overview.

## 9. Sửa lỗi số lượng tham số placeholders (?) khi Import Excel
*   **Vấn đề phát hiện**: Khi tải file Excel lịch học lên, hệ thống báo lỗi `Failed to parse and save schedules: 13 values for 12 columns`.
*   **Nguyên nhân**: Câu lệnh INSERT vào bảng `lab_schedules` khai báo 12 cột dữ liệu nhưng phần `VALUES` lại chứa 13 dấu hỏi chấm `?`.
*   **Các tệp đã chỉnh sửa**:
    *   `src/Newest_Version/api_server.py`: Chỉnh sửa danh sách placeholders trong câu lệnh `INSERT INTO lab_schedules` thành đúng 12 dấu hỏi chấm `?` tương ứng với số lượng cột và dữ liệu đầu vào.

## 10. Tăng cường Độ tương phản & Khả năng Đọc (Readability) trên giao diện
*   **Mục tiêu**: Điều chỉnh lại những phần chữ màu xám nhạt (như nhãn phụ, tiêu đề phụ, mô tả...) vốn khó nhìn do màu gần giống màu nền và nét chữ quá mỏng thành màu đen đậm, rõ nét hơn.
*   **Các tệp đã chỉnh sửa**:
    *   `web_app/src/index.css`:
        *   Tích hợp các CSS class overrides đè trực tiếp lên các mã màu xám nhạt của Tailwind (`#94a3b8` và `#475569`).
        *   Tự động đổi màu tất cả các văn bản này sang màu đen/xám tối (`#0f172a`) và nâng độ đậm (font-weight) tương ứng lên mức `font-bold` (700) và `font-semibold` (600).
        *   Giải pháp này đảm bảo tất cả các trang con trong hệ thống đều tự động hiển thị chữ rõ nét và có độ tương phản tuyệt đối trên mọi màn hình.




