# Báo Cáo Thay Đổi Hệ Thống Access Control (Ngày 07/07/2026)

Tài liệu này tóm tắt toàn bộ các tính năng, nâng cấp và sửa đổi đã thực hiện trong phiên làm việc hôm nay để làm cơ sở thông tin cho phiên tiếp theo.

---

## 1. Giao Diện Admin Web-App Đổi Màu Trắng - Cam (White-Orange Light Theme)
- **Mục tiêu**: Chuyển toàn bộ hệ thống giao diện quản lý admin từ tông tối (Dark Mode) sang tông màu sáng trắng - cam cao cấp và hiện đại hơn.
- **Các tệp đã chỉnh sửa**:
  - `web_app/tailwind.config.js` (Thêm định nghĩa mã màu cam `green: '#ff6d00'`, viền `line`, text và nền).
  - `web_app/src/index.css` (Cập nhật màu nền mặc định `bg-slate-100` và text mặc định `text-slate-900`).
  - Các trang layout chính: `OverviewPage.tsx`, `UsersPage.tsx`, `LogsPage.tsx`, `SystemPage.tsx`, `ControlPage.tsx`, `LabSelectorPage.tsx`, `LoginPage.tsx`, `App.tsx`.
  - Các component dùng chung: `Panel.tsx`, `Badge.tsx`, `Button.tsx`, `Sidebar.tsx`, `TopBar.tsx`.
- **Kết quả**: Giao diện sáng đồng nhất, các điểm nhấn/nút nổi bật với tông màu cam, loại bỏ hoàn toàn các màu nền tối cứng.

---

## 2. Nâng Cấp Trang Đăng Ký Người Dùng (Enrollment Page)
- **Chỉnh sửa ảnh slot đã chụp**: Cho phép người dùng nhấn lại vào các slot ảnh sinh trắc học đã chấp nhận trước đó trên `EnrollmentPage.tsx` để chụp lại/ghi đè nếu chưa vừa ý.
- **Bổ sung Validation (Chặn bước)**:
  - Không cho phép nhấn "Next" sang Bước 2 nếu chưa điền đầy đủ thông tin danh tính (Tên, mã PIN, mã RFID).
  - Không cho phép hoàn tất đăng ký ở Bước 3 nếu chưa chụp đủ 3 ảnh sinh trắc học bắt buộc.

---

## 3. API Thu Hồi Quyền Truy Cập (Revoke Access / Delete User)
- **Mục tiêu**: Cho phép xóa người dùng khỏi hệ thống quản lý tập trung và đồng bộ về các thiết bị Edge.
- **Các tệp đã chỉnh sửa**:
  - `src/Newest_Version/api_server.py` (Bổ sung route `DELETE /api/labs/<lab_id>/users/<user_id>` để xóa khỏi DB SQLite và xóa toàn bộ các tệp ảnh gốc trên máy chủ).
  - `web_app/src/lib/db.ts` (Thêm hàm gọi API `deleteUser`).
  - `web_app/src/store/adminStore.ts` (Kết nối action `deleteUser` vào Zustand store toàn cục).
  - `web_app/src/pages/UsersPage.tsx` (Liên kết nút **Revoke Access** trong bảng quản lý người dùng để kích hoạt xóa).

---

## 4. Tính Năng Livestream Camera Hồng Ngoại (On-Demand IR Live View)
- **Mục tiêu**: Cho phép admin theo dõi luồng video trực tiếp từ Camera IR trên giao diện Admin để căn chỉnh góc hoặc giám sát, đạt tốc độ thực tế (15-20 FPS) mà không gây hại ổ đĩa.
- **Cơ chế hoạt động**:
  - **In-Memory Streaming (Zero Disk Wear)**: Ảnh được nén dạng JPEG trong bộ nhớ RAM và truyền đi dạng luồng HTTP thô. Hoàn toàn **không ghi bất kỳ tệp tin nào lên thẻ SD/SSD** của Raspberry Pi 5 để tối đa hóa tuổi thọ phần cứng.
  - **Native MJPEG Stream**: Máy chủ Flask phục vụ một endpoint kiểu `multipart/x-mixed-replace` trích xuất từ bộ đệm RAM. Trình duyệt hiển thị luồng ảnh này trực tiếp qua thẻ `<img>` mà không cần gọi API hẹn giờ (no polling), cho tốc độ khung hình đạt 15-20 FPS mượt mà.
- **Các tệp đã chỉnh sửa**:
  - `src/Newest_Version/api_server.py`:
    - Khởi tạo bộ đệm in-memory `latest_frames`.
    - Route `POST /ir-frame` nhận dữ liệu ảnh nhị phân trực tiếp.
    - Route `GET /ir-stream` phục vụ luồng MJPEG thời gian thực, tự động giải phóng tài nguyên khi admin đóng tab.
  - `src/Newest_Version/local_sync_client.py`: Chỉ đóng vai trò bật/tắt cờ yêu cầu livestream `ir_stream_active.txt`, chu kỳ đồng bộ giữ nguyên 4 giây/lần.
  - `src/Newest_Version/app.py`: Thêm luồng chạy ngầm daemon `self.stream_thread` liên tục đọc cờ, nén ảnh trong RAM và POST lên Server trung tâm ở tốc độ 15 FPS khi cờ hoạt động. Loại bỏ hoàn toàn mã lưu file xuống đĩa trong callback của GStreamer.
  - `web_app/src/components/ui/LiveCamera.tsx`: Tạo mới component HUD Live Camera kết nối trực tiếp tới luồng stream MJPEG.
  - `web_app/src/pages/SystemPage.tsx`: Nhúng thẻ hiển thị Live Camera hồng ngoại lên cột bên phải.

---

## 5. Sửa lỗi hiển thị trạng thái Online/Offline của Edge Node
- **Mục tiêu**: Khắc phục lỗi khi tắt chương trình nhận diện khuôn mặt (`app.py`), Web-app vẫn báo thiết bị "Online" (do tiến trình đồng bộ `local_sync_client.py` chạy độc lập vẫn gửi trạng thái cũ lưu trong SQLite).
- **Các tệp đã chỉnh sửa**:
  - `src/Newest_Version/local_sync_client.py`: So sánh thời điểm cập nhật cơ sở dữ liệu gần nhất (`updatedAt`) của `app.py`. Nếu vượt quá 12 giây, tự động báo trạng thái camera/nhận diện là `offline` về Flask Server.
  - `src/Newest_Version/api_server.py`: Trong route lấy danh sách các node (`/api/labs/<lab_id>/clusters/<cluster_id>/nodes`), nếu quá 15 giây không nhận được dữ liệu đo lường từ node, máy chủ sẽ tự động kết luận node đó đã ngoại tuyến (`onlineState = "offline"`, `modelStatus = "stopped"`).
  - `web_app/src/App.tsx`: Tích hợp banner cảnh báo trực quan toàn cục ngay bên dưới thanh điều hướng TopBar (Màu đỏ nếu thiết bị Offline hoàn toàn, Màu cam nếu chỉ dừng chương trình nhận diện khuôn mặt). giúp admin nhận biết ngay bất thường từ bất kỳ trang nào.

---

## 6. Đề Xuất Giải Pháp Quản Lý và Tối Ưu RAM cho Thiết Bị Edge 2GB RAM
Để đảm bảo hệ thống vận hành cực kỳ ổn định trên các dòng Raspberry Pi 5 chỉ có 2GB RAM khi chạy đồng thời cả mô hình AI nhận diện khuôn mặt và luồng phát livestream camera IR, các giải pháp đề xuất bao gồm:

### A. Hạ độ phân giải luồng quét Camera IR
- **Chi tiết**: Hạ độ phân giải luồng chụp camera IR trong chuỗi pipeline GStreamer của `app.py` từ `640x480` xuống `320x240`.
- **Hiệu quả**: 
  - Dung lượng RAM chứa ảnh thô giảm 4 lần (chỉ còn ~76 KB).
  - Tải CPU khi nén ảnh JPEG giảm sâu, dung lượng gói tin tải lên Server chỉ còn < 8 KB.
  - Vẫn đáp ứng hoàn hảo độ chính xác cho Haar Cascade phát hiện mặt và các phép tính toán liveness kiểm tra giả mạo.

### B. Gọi cơ chế giải phóng RAM chủ động (Garbage Collection)
- **Chi tiết**: Sử dụng module `gc` của Python và gọi `gc.collect()` định kỳ trong luồng phụ gửi ảnh (`_stream_sender_loop`).
- **Hiệu quả**: Ép Python giải phóng vùng nhớ của các mảng ảnh cũ ngay lập tức sau khi hoàn thành kết nối mạng, tránh hiện tượng rò rỉ hoặc giữ RAM không cần thiết.

### C. Tái sử dụng Bộ đệm cố định (Pre-allocated JPEG Buffer)
- **Chi tiết**: Khai báo trước một mảng bộ đệm `self._jpeg_buffer = np.zeros(...)` trong RAM lúc khởi tạo. Hàm `cv2.imencode` sẽ ghi đè trực tiếp lên mảng có sẵn thay vì liên tục xin cấp phát vùng nhớ động mới cho mỗi khung hình.
- **Hiệu quả**: Loại bỏ xung đột phân mảnh bộ nhớ động trên hệ thống nhúng.

---

## 7. Việt hóa toàn bộ giao diện và nhật ký sang Tiếng Anh (English Localization)
- **Mục tiêu**: Việt hóa toàn bộ các câu thông báo, cảnh báo lỗi, trạng thái xác thực và nhật ký in ra terminal từ tiếng Việt sang tiếng Anh theo chuẩn chung.
- **Các tệp đã chỉnh sửa**:
  - `web_app/src/App.tsx`: Chuyển đổi nội dung của hai thanh cảnh báo toàn cục (`System Alert` và `Service Alert`) sang tiếng Anh.
  - `src/monitor_display/interface_monitor.py`: Dịch toàn bộ trạng thái hiển thị trên màn hình 7-inch (như `Look Straight`, `Turn Left`, `Security Warning`, `Access Denied`, `Spoof Detected`).
  - `src/Newest_Version/register.py`: Dịch toàn bộ thông tin in ra console khi quét khuôn mặt/đăng ký offline sang tiếng Anh.
  - `src/Newest_Version/firebase_sync.py`: Dịch các log tải ảnh và kết nối Firebase sang tiếng Anh.
  - `src/Newest_Version/database.py`: Chuyển nhật ký lưu/xóa tài khoản của database logger sang tiếng Anh.

---

## 8. Tối ưu hóa cơ chế đồng bộ thời gian thực cho các Node (Real-time Status Sync Optimization)
- **Mục tiêu**: Nâng cấp cơ chế đồng bộ và giảm độ trễ hiển thị trạng thái kết nối của các Raspberry Pi Node trên giao diện Web-App xuống 2-3 giây.
- **Giải pháp**:
  - **Tách tần suất chạy của Sync Client (`local_sync_client.py`)**: 
    - Gửi gói tin telemetry/heartbeat cực nhẹ định kỳ mỗi `2` giây một lần (thay vì 4 giây như trước).
    - Đồng bộ log truy cập/sự cố định kỳ mỗi `4` giây.
    - Đồng bộ dữ liệu người dùng và cấu hình từ máy chủ định kỳ mỗi `15` giây (tránh làm quá tải RAM và CPU của Raspberry Pi 2GB).
  - **Tối ưu hóa thời gian ngắt kết nối (Watchdog Timeout)**:
    - Tại Node: Giảm thời gian kiểm tra trạng thái tiến trình `app.py` từ `12.0` giây xuống còn `5.0` giây. Nếu ứng dụng dừng hoạt động, Node sẽ gửi trạng thái Offline ngay trong vòng 2 giây.
    - Tại Central Server (`api_server.py`): Giảm giới hạn kiểm tra tín hiệu sống của Node từ `15.0` giây xuống còn `5.0` giây. Đảm bảo nếu Node mất kết nối hoặc tắt nguồn đột ngột, Web-App sẽ tự động cập nhật và hiển thị Offline trong vòng tối đa 5 giây.
- **Các tệp đã chỉnh sửa**:
  - `src/Newest_Version/local_sync_client.py`
  - `src/Newest_Version/api_server.py`

---

## 9. Tích hợp ổ đĩa HDD ngoài để lưu trữ cơ sở dữ liệu và nhật ký (External HDD Storage Integration)
- **Mục tiêu**: Tận dụng 1 ổ cứng HDD 500GB khỏe trên máy chủ Proxmox để làm phân vùng lưu trữ cho toàn bộ Cơ sở dữ liệu và Logs của container Docker, đồng thời khắc phục lỗi treo boot của máy chủ vật lý.
- **Giải pháp**:
  - **Khắc phục lỗi treo boot (Emergency Mode)**: Khi một ổ đĩa trong `/etc/fstab` bị lỗi phần cứng hoặc mất kết nối, hệ điều hành Linux mặc định sẽ chặn tiến trình boot. Giải pháp là thêm cờ `nofail` vào tham số mount để hệ thống tiếp tục khởi động bình thường ngay cả khi ổ cứng gặp sự cố.
  - **Cấu hình trên Host (`vcl`)**:
    - Chỉ mount ổ đĩa khỏe `/dev/sdb` vào đường dẫn `/mnt/smart-door/database`.
    - Tạo thư mục con `/mnt/smart-door/database/logs` trực tiếp trên ổ `sdb` này để lưu trữ logs.
    - Chỉnh sửa `/etc/fstab` loại bỏ hoàn toàn ổ đĩa lỗi `sdc`, và cấu hình dòng mount cho `sdb` dưới dạng:
      `UUID=9ddcaf43-7ba6-41da-b97f-4a2fbe41499b    /mnt/smart-door/database    ext4    defaults,nofail    0    2`
    - Xóa điểm kết nối lỗi (`mp1`) và cập nhật điểm kết nối (`mp0`) của LXC Container 100 bằng lệnh `pct`.
  - **Cấu hình Docker Compose**:
    - Ánh xạ volume trong `docker-compose.yml` để liên kết thư mục database chính và thư mục con logs tương ứng:
      ```yaml
      volumes:
        - /mnt/smart-door/database:/app/database
        - /mnt/smart-door/database/logs:/app/logs
      ```
- **Các tệp đã chỉnh sửa**:
  - `docker-compose.yml`

---

## 10. Tích hợp cơ sở dữ liệu Vector Qdrant cho hệ thống nhận diện khuôn mặt (Qdrant Vector Database Integration)
- **Mục tiêu**: Nâng cấp kiến trúc dữ liệu từ SQLite thuần túy lên mô hình Hybrid (SQLite kết hợp cơ sở dữ liệu vector chuyên dụng Qdrant) để tối ưu hóa thời gian tìm kiếm khuôn mặt, tăng khả năng mở rộng lên hàng triệu người dùng và ngăn ngừa xung đột lock file khi có nhiều Node Pi ghi log đồng thời.
- **Giải pháp**:
  - **Dịch vụ Qdrant trên Docker**: Cấu hình thêm dịch vụ Qdrant chạy độc lập trong `docker-compose.yml`, lưu trữ index vector HNSW trực tiếp lên ổ cứng HDD khỏe `/mnt/smart-door/database/qdrant_storage`.
  - **Tự động di trú dữ liệu (Auto-Backfill)**: Khi Server khởi động, một luồng nền sẽ tự động quét cơ sở dữ liệu SQLite hiện tại, chuyển đổi các vector nhị phân thành float vector và đẩy lên Qdrant, đảm bảo không mất mát dữ liệu người dùng cũ.
  - **Đồng bộ hóa vòng đời**:
    - Khi đăng ký/nạp vector mới (`POST /api/users/<full_name>/embedding`), hệ thống sẽ ghi vào SQLite đồng thời gọi `upsert` lưu vector vào Qdrant với ID người dùng tương ứng.
    - Khi xóa người dùng (`DELETE /api/labs/<lab_id>/users/<user_id>`), hệ thống sẽ tự động gọi lệnh xóa vector tương ứng trong Qdrant.
  - **API nhận diện khuôn mặt**: Bổ sung endpoint `POST /api/users/identify` để so khớp khuôn mặt qua vector nhanh gấp 100 lần bằng chỉ mục HNSW của Qdrant.
- **Các tệp đã chỉnh sửa**:
  - `docker-compose.yml`
  - `Dockerfile`
  - `src/Newest_Version/api_server.py`
