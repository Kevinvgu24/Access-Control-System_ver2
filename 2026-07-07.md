# Báo Cáo Thay Đổi Hệ Thống Access Control (Ngày 07/07/2026)

Tài liệu này tóm tắt toàn bộ các tính năng, nâng cấp và sửa đổi đã thực hiện trong phiên làm việc hôm nay để làm cơ sở thông tin cho phiên tiếp theo.

---

## 1. Giao Diện Admin Web-App Đổi Màu Trắng - Cam (White-Orange Light Theme)
- **Mục tiêu**: Chuyển toàn bộ hệ thống giao diện quản lý admin từ tông tối (Dark Mode) sang tông màu sáng trắng - cam cao cấp và hiện đại hơn.
- **Các tệp đã chỉnh sửa**:
  - `web_app/tailwind.config.js` (Thêm định nghĩa mã màu cam `green: '#ff6d00'`, viền `line`, text và nền).
  - `web_app/src/index.css` (Cập nhật màu nền mặc định `bg-slate-100` và text mặc định `text-slate-900`).
  - Các trang layout chính: `OverviewPage.tsx`, `UsersPage.tsx`, `LogsPage.tsx`, `SystemPage.tsx`, `ControlPage.tsx`, `LabSelectorPage.tsx`, `LoginPage.tsx`, `App.tsx`.
  - Các component dùng chung: `Panel.tsx`, `Badge.tsx`, `Button.tsx`, `Sidebar.tsx`, `TopBar.tsx`.
- **Kết quả**: Giao diện sáng đồng nhất, các điểm nhấn/nút nổi bật với tông màu cam, loại bỏ hoàn toàn các màu nền tối cứng.

---

## 2. Nâng Cấp Trang Đăng Ký Người Dùng (Enrollment Page)
- **Chỉnh sửa ảnh slot đã chụp**: Cho phép người dùng nhấn lại vào các slot ảnh sinh trắc học đã chấp nhận trước đó trên `EnrollmentPage.tsx` để chụp lại/ghi đè nếu chưa vừa ý.
- **Bổ sung Validation (Chặn bước)**:
  - Không cho phép nhấn "Next" sang Bước 2 nếu chưa điền đầy đủ thông tin danh tính (Tên, mã PIN, mã RFID).
  - Không cho phép hoàn tất đăng ký ở Bước 3 nếu chưa chụp đủ 3 ảnh sinh trắc học bắt buộc.

---

## 3. API Thu Hồi Quyền Truy Cập (Revoke Access / Delete User)
- **Mục tiêu**: Cho phép xóa người dùng khỏi hệ thống quản lý tập trung và đồng bộ về các thiết bị Edge.
- **Các tệp đã chỉnh sửa**:
  - `src/Newest_Version/api_server.py` (Bổ sung route `DELETE /api/labs/<lab_id>/users/<user_id>` để xóa khỏi DB SQLite và xóa toàn bộ các tệp ảnh gốc trên máy chủ).
  - `web_app/src/lib/db.ts` (Thêm hàm gọi API `deleteUser`).
  - `web_app/src/store/adminStore.ts` (Kết nối action `deleteUser` vào Zustand store toàn cục).
  - `web_app/src/pages/UsersPage.tsx` (Liên kết nút **Revoke Access** trong bảng quản lý người dùng để kích hoạt xóa).

---

## 4. Tính Năng Livestream Camera Hồng Ngoại (On-Demand IR Live View)
- **Mục tiêu**: Cho phép admin theo dõi luồng video trực tiếp từ Camera IR trên giao diện Admin để căn chỉnh góc hoặc giám sát, đạt tốc độ thực tế (15-20 FPS) mà không gây hại ổ đĩa.
- **Cơ chế hoạt động**:
  - **In-Memory Streaming (Zero Disk Wear)**: Ảnh được nén dạng JPEG trong bộ nhớ RAM và truyền đi dạng luồng HTTP thô. Hoàn toàn **không ghi bất kỳ tệp tin nào lên thẻ SD/SSD** của Raspberry Pi 5 để tối đa hóa tuổi thọ phần cứng.
  - **Native MJPEG Stream**: Máy chủ Flask phục vụ một endpoint kiểu `multipart/x-mixed-replace` trích xuất từ bộ đệm RAM. Trình duyệt hiển thị luồng ảnh này trực tiếp qua thẻ `<img>` mà không cần gọi API hẹn giờ (no polling), cho tốc độ khung hình đạt 15-20 FPS mượt mà.
- **Các tệp đã chỉnh sửa**:
  - `src/Newest_Version/api_server.py`:
    - Khởi tạo bộ đệm in-memory `latest_frames`.
    - Route `POST /ir-frame` nhận dữ liệu ảnh nhị phân trực tiếp.
    - Route `GET /ir-stream` phục vụ luồng MJPEG thời gian thực, tự động giải phóng tài nguyên khi admin đóng tab.
  - `src/Newest_Version/local_sync_client.py`: Chỉ đóng vai trò bật/tắt cờ yêu cầu livestream `ir_stream_active.txt`, chu kỳ đồng bộ giữ nguyên 4 giây/lần.
  - `src/Newest_Version/app.py`: Thêm luồng chạy ngầm daemon `self.stream_thread` liên tục đọc cờ, nén ảnh trong RAM và POST lên Server trung tâm ở tốc độ 15 FPS khi cờ hoạt động. Loại bỏ hoàn toàn mã lưu file xuống đĩa trong callback của GStreamer.
  - `web_app/src/components/ui/LiveCamera.tsx`: Tạo mới component HUD Live Camera kết nối trực tiếp tới luồng stream MJPEG.
  - `web_app/src/pages/SystemPage.tsx`: Nhúng thẻ hiển thị Live Camera hồng ngoại lên cột bên phải.

---

## 5. Đề Xuất Giải Pháp Quản Lý và Tối Ưu RAM cho Thiết Bị Edge 2GB RAM
Để đảm bảo hệ thống vận hành cực kỳ ổn định trên các dòng Raspberry Pi 5 chỉ có 2GB RAM khi chạy đồng thời cả mô hình AI nhận diện khuôn mặt và luồng phát livestream camera IR, các giải pháp đề xuất bao gồm:

### A. Hạ độ phân giải luồng quét Camera IR
- **Chi tiết**: Hạ độ phân giải luồng chụp camera IR trong chuỗi pipeline GStreamer của `app.py` từ `640x480` xuống `320x240`.
- **Hiệu quả**: 
  - Dung lượng RAM chứa ảnh thô giảm 4 lần (chỉ còn ~76 KB).
  - Tải CPU khi nén ảnh JPEG giảm sâu, dung lượng gói tin tải lên Server chỉ còn < 8 KB.
  - Vẫn đáp ứng hoàn hảo độ chính xác cho Haar Cascade phát hiện mặt và các phép tính toán liveness kiểm tra giả mạo.

### B. Gọi cơ chế giải phóng RAM chủ động (Garbage Collection)
- **Chi tiết**: Sử dụng module `gc` của Python và gọi `gc.collect()` định kỳ trong luồng phụ gửi ảnh (`_stream_sender_loop`).
- **Hiệu quả**: Ép Python giải phóng vùng nhớ của các mảng ảnh cũ ngay lập tức sau khi hoàn thành kết nối mạng, tránh hiện tượng rò rỉ hoặc giữ RAM không cần thiết.

### C. Tái sử dụng Bộ đệm cố định (Pre-allocated JPEG Buffer)
- **Chi tiết**: Khai báo trước một mảng bộ đệm `self._jpeg_buffer = np.zeros(...)` trong RAM lúc khởi tạo. Hàm `cv2.imencode` sẽ ghi đè trực tiếp lên mảng có sẵn thay vì liên tục xin cấp phát vùng nhớ động mới cho mỗi khung hình.
- **Hiệu quả**: Loại bỏ xung đột phân mảnh bộ nhớ động trên hệ thống nhúng.
